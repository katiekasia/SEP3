syntax = "proto3";

option java_multiple_files = true;
option java_package = "loginPatient.grpc";

service LoginPatient {
  rpc loginPatient (LoginRequestPatient) returns (loginResponsePatient) {}
}

message LoginRequestPatient {
  string cpr = 1;
  string password = 2;
}


message loginResponsePatient {
  string confirmation = 1;
}